plugins {
    id 'java'
}

sourceCompatibility = 1.8
targetCompatibility = 1.8

tasks.withType(JavaCompile) {
    options.encoding = 'UTF-8'
}

version '1.0.0-Alpha'

repositories {
    mavenCentral()
}

dependencies {
    compile fileTree(include: ['spigot-1.8-R0.1-SNAPSHOT.jar'], dir: 'Server')
}